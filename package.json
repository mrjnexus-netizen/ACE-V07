{
  "name": "ace-v07-root",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22.11.0",
    "pnpm": "9.12.3"
  },
  "packageManager": "pnpm@9.12.3",
  "scripts": {
    "dev:frontend": "pnpm --filter @workspace/ace-2026 run dev",
    "dev:api": "pnpm --filter @workspace/api-server run dev",
    "typecheck": "pnpm -r run typecheck",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "build": "pnpm -r run build",
    "db:migrate": "pnpm --filter @workspace/api-server run db:migrate",
    "db:seed": "pnpm --filter @workspace/api-server run db:seed",
    "db:seed-content": "pnpm --filter @workspace/api-server run db:seed-content",
    "db:studio": "pnpm --filter @workspace/api-server run db:studio",
    "test:e2e": "pnpm --filter @workspace/ace-2026 run test:e2e"
  },
  "devDependencies": {
    "@types/uuid": "^11.0.0",
    "typescript": "5.7.2"
  },
  "pnpm": {
    "overrides": {
      "three": "0.170.0"
    }
  }
}